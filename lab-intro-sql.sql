# 2. select * from sakila.`actor`,sakila.`address`,sakila.`category`, ... 

# 3. select `title` from `film_list`

# 4. select `name` from `language` as languag

# 5. select COUNT(DISTINCT(`store_id`)) from `store` as store_total
 
# 5. select COUNT(DISTINCT(`staff_id`)) from `staff` as staff_total

# 5. select `first_name` from `staff`

# Bonus : select COUNT(DISTINCT`rental_date`) from `rental`